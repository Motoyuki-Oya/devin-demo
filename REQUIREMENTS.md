# 阿部寛・アルティメット・エディション（1000万同時接続対応）要件定義書

## 1. プロジェクト概要
*   **目的**: 1000万規模のクライアント同時接続を処理可能な、超低遅延・高スループットな分散システム基盤の構築。
*   **コンセプト**: 「阿部寛のホームページ」級の爆速表示と、MMO規模のリアルタイム通信を両立。

## 2. システム構成・アーキテクチャ

### 2.1. クライアント & フロントエンド
*   **静的コンテンツ**: Astro (海)。Cloudflareエッジから0.1秒で配信。
*   **動的処理**: SolidJS + WebAssembly (島)。計算負荷をクライアントへオフロード。
*   **通信プロトコル**:
    *   **メイン**: WebTransport (HTTP/3 UDP)。
    *   **フォールバック**: WebSocket (TCP/HTTP) for Proxy環境。

### 2.2. エッジ & ルーティング
*   **プラットフォーム**: Cloudflare Workers。
*   **ルーティング**: ユーザーIDに基づき、特定のバックエンド「Cell」へ固定ルーティング (Sticky Session)。
    *   *Note*: AWS NLB等のL4ロードバランサでは細かいSticky制御が困難なため、アプリケーション層であるWorkersで制御する。
*   **防御**: WAF, Bot対策, 規約同意スクロールによるトラフィック分散（詳細は「4. セキュリティ要件」参照）。

### 2.3. バックエンド (Cell Core)
*   **役割**: WebSocket/WebTransport 接続の終端、状態管理。
*   **構成単位**: "Cell" と呼ばれる独立した処理ユニット。
*   **技術スタック**:
    *   **言語**: Kotlin + Java 25 (GraalVM)。
    *   **フレームワーク**: Quarkus (Nativeコンパイル対応)。
    *   **並行処理**: 仮想スレッド (Project Loom) による高密度接続維持。
    *   **シリアライゼーション**: Protobuf (バイナリ通信)。

### 2.4. データストア & インフラ
*   **リアルタイム層**: **Dragonfly** (Redis互換)。マルチスレッドアーキテクチャにより、セッション情報や一時的な状態を超高速に処理。
*   **永続化層**: Amazon Aurora (RDB)。SQSを介した非同期書き込みでスパイクを吸収。
*   **サーバー**: AWS EC2 (Graviton3 / ARMアーキテクチャ)。
*   **LB**: AWS Network Load Balancer (NLB) + Pre-warming。

## 3. 非機能要件

### 3.1. スケーラビリティ（接続数目標）
*   **全体目標**: 同時接続数 **1,000万**。
*   **実験目標**:
    *   Cell数: **1セル** から開始し、最終的に **4セル** で検証。
    *   目標: 1セルあたり **250万接続** の処理能力確認。
*   **本番構成 (想定)**:
    *   サーバー台数: **200台**。
    *   1台あたりの目安: 5万〜10万接続（安全マージン確保）。

### 3.2. 性能目標（SLO）
*   **接続確立**: WebSocket/WebTransport ハンドシェイク完了まで **< 100ms**（p95）。
*   **往復遅延（RTT）**: メッセージ送信〜エコー受信まで **< 50ms**（p95、同一リージョン）。
*   **スループット**: 1セルあたり **10万 msgs/sec** 以上を処理可能であること。
*   **可用性**: 月間 **99.9%** 以上（計画メンテナンス除く）。

### 3.3. スパイク耐性
*   短時間の接続集中（再接続嵐、イベント開始など）に耐えること。
*   急激な負荷増加時も、既存接続への影響を最小化すること。

### 3.4. 可用性・障害耐性
*   **前提**: Cell は水平スケール前提で、単体障害を許容する。
*   **要求**:
    *   Cell 障害時にクライアントが再接続できること。
    *   ルーティングが失敗した場合のフォールバック動作（別Cell割り当て、リトライ、エラーメッセージ）を定義すること。
    *   Dragonfly 障害/遅延時に、致命的に全接続が巻き込まれない設計（タイムアウト、劣化運転）を行うこと。

### 3.5. ステートの扱い（整合性）
*   **オンライン状態**:
    *   TTL（失効）を前提として「最終的に正しい」運用ができること。
*   **セッション情報**:
    *   どこまでをメモリ（Cell内）に持ち、どこからを外部（Dragonfly/Aurora）に置くかの境界を定義すること。

## 4. セキュリティ要件

### 4.1. エッジ防御（WAF / Bot対策）
*   **WAF**: Cloudflare WAF を有効化し、既知の攻撃パターンをブロックする。
*   **Bot対策**: Cloudflare Bot Management または同等機能で、自動化されたアクセスを検知・制限する。
*   **トラフィック分散**: 規約同意スクロール等の仕組みで、イベント開始時の瞬間アクセス集中を緩和する。

### 4.2. 認証・認可
*   **接続時の本人確認**:
    *   `userId` をURLに含める場合、なりすまし対策（署名付きトークン等）を必須とする。
*   **権限制御**:
    *   送受信できるイベント/チャネルをユーザー権限により制御できること。

### 4.3. 通信の保護
*   **TLS**:
    *   本番は必須。
    *   ローカル/検証環境でも WebTransport（HTTP/3）を扱う場合は証明書運用を用意する。
*   **レート制限**:
    *   接続/再接続、メッセージ送信についてエッジまたはCell側で制限できること。

## 5. 運用・監視（Observability）要件

### 5.1. ログ
*   **相関ID**:
    *   Edge で付与する `routing_id` 等をログに残し、エッジ〜セル間の追跡ができること。
*   **エラー分類**:
    *   接続失敗、認証失敗、バックプレッシャー、Redis遅延などを分類して記録できること。

### 5.2. メトリクス
*   **収集すべき指標**（3.2 SLO 判定に使用）:
    *   同時接続数（セル別）
    *   接続成功/失敗率、切断理由別カウント
    *   接続確立時間（p50/p95/p99）
    *   メッセージ処理レイテンシ（p50/p95/p99）
    *   Dragonfly のコマンドレイテンシ、エラー率

### 5.3. 負荷試験の合格条件
*   **合否判定基準**（3.2 SLO を基準とする）:
    *   目標接続数を **30分以上** 維持して接続が安定すること。
    *   p95 レイテンシが SLO 内に収まること。
    *   再接続嵐シナリオでシステム全体が崩壊しないこと（エラーが局所化する）。
*   具体的な実施タスクは「6. 開発ロードマップ & ToDo」参照。

## 6. 開発ロードマップ & ToDo
*   [x] **基盤構築**:
    *   [x] Java 25 (GraalVM) 導入。
    *   [x] Dragonfly (Redis互換) 準備。
*   [ ] **1セル実験準備**:
    *   [x] WebTransport用証明書 (mkcert) 作成。
    *   [x] OSチューニング (ulimit, UDPバッファ) (Skipped for Local Windows)。
*   [ ] **1セル負荷試験**:
    *   [ ] モンスターPC (128GB RAM) での接続試験。
    *   [ ] k6 (複数IP) を用いた負荷ベンチマーク。
    *   [ ] 合格条件は「5.3. 負荷試験の合格条件」を満たすこと。
*   [ ] **分散構成(4セル)検証**:
    *   [ ] Cloudflare Workers によるSticky Sessionルーティング検証。
